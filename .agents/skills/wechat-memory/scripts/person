#!/usr/bin/env bash
set -euo pipefail
command -v wechat-memory >/dev/null 2>&1 || { echo "wechat-memory 不在 PATH" >&2; exit 127; }
if [[ $# -ne 1 ]]; then
  echo '用法: person "姓名"' >&2
  exit 2
fi
exec wechat-memory person "$1"
